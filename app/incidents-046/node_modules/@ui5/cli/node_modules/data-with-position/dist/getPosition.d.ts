import { DataWithPosition, Position } from "./types";
interface GetPosition {
    (dataWithPosition: DataWithPosition): Position;
    (dataWithPosition: undefined): undefined;
}
export declare const getPosition: GetPosition;
export {};
